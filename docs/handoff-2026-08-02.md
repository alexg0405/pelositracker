# Session handoff — 2026-08-02 (end of day)

Continues `handoff-2026-08-01.md`. Everything below is committed on `main`
and pushed (`origin/main` at `f5efcac`); the old working branch
`local/polymarket-us-research` retains the granular 33-commit history
locally. Suite: **678 passed, 1 skipped**, ruff clean.

## State right now

- **Server is running** (port 8775, started by the operator via
  `start-workstation-app.cmd`; normal visible-window workflow restored —
  Ctrl+C in that window is the proper shutdown).
- **Autopilot is armed and active** for the Aug 2 slate; it shuts polling
  and dry automation off after the last final, as designed.
- **Live lane** (operator-tuned, real money, ~$25 allocation): reversal
  confirmation **5** readings, daily-loss stop $10, band 23–45c, all three
  lines with per-line profiles — ML/all pt 0.30; spread/all pt 0.20 (and an
  operator-added spread/late enabled profile); totals **early only** pt
  0.20 (middle/late/all disabled). Operator edits arrive frequently through
  the UI — always read the current policy from the DB before reasoning
  about behavior; never assume staged values survived.
- **Dry lane** (measurement bed): reversal readings **3**, all lines, band
  25–49c (operator-widened), totals early+middle (late blocked), pt 0.30
  everywhere, effectively uncapped losses by design.
- **The live-5 vs dry-3 reversal-readings A/B is live on tonight's slate.**

## The morning readout (first thing next session)

All of tonight's open questions are settled by one pass:

```bash
./.venv/Scripts/python.exe -m tools.backfill_event_outcomes --apply
./.venv/Scripts/python.exe -m tools.settlement_counterfactual workstation-data/polymarket-us-dry-run.db
./.venv/Scripts/python.exe -m tools.settlement_counterfactual polymarket-us-trading.db
```

Filter to the current regime by `entry_policy_json` stop_loss == 0.5 and
decompose by exit reason and line. Questions the data now answers:

1. **Reversal window: 3 vs 5 readings** — compare lanes per line. Earlier
   slice showed even 3-reading-confirmed reversals recovering (+$59 held on
   5 dry exits) but the same night the locks saved $97.58 on a weak-entry
   evening; one night flips this, the graded whole decides.
2. **Totals early/middle economics** under the new stack (first evening:
   2-0, +34% realized, one big open mark; stage guard verified — zero late
   entries). If middle grades well, the operator wants it opened on live.
3. **Spread entry quality** — spreads had a weak-entry night with the
   exits *protecting* (actual beat held). Watch whether that persists.
4. **Live ML winners-cut** — one slice showed 5/5 settlement winners
   realized at a loss (stops+reversals cut all five). Per-line
   stop-vs-hold needs the full grading before touching stop settings.

## What landed today (all in `5d4c52f` + `f5efcac`)

- **Reversal confirmation** (`reversal_confirmation_readings`, migration
  v16, UI field, journal telemetry) — the third one-quote exit leak, fixed
  the same way as the stop. Baseline it must beat: 62–65% of single-reading
  reversal sales were settlement winners, $93 giveback in one night.
- **Config API whitelist bug fixed**: `LiveTradingConfigIn` silently
  dropped fields not listed (the "readings stick at 3" mystery). Any new
  policy field MUST be added there or UI saves discard it; a test now pins
  this.
- **Journal retention fixed**: rejection dedup keys normalize numbers;
  qualification chatter evicts before marks. Mark evidence (edge paths,
  now run-expectancy/pitcher shadow) survives for replays.
- **MLB shadow telemetry**: every mark carries base/out run expectancy
  (RE24), count, batter/pitcher; pitcher changes journaled
  (kind=`shadow`). Strictly observational — first graduation candidate
  after a slate of evidence: "do adverse moves follow bullpen changes?"
- **Totals re-authorized** with stage guards after per-stage grading showed
  late-stage totals are the only consistently toxic pocket (−9 to −12pp,
  −36 to −61% held, both lanes); early/middle lean positive.
- **Storage policy enforced**: hosted (website) runs persist everything to
  `DATABASE_URL` (Supabase); workstation runs discard `DATABASE_URL` at
  startup and stay on local SQLite always. `tools/migrate_to_supabase.py`
  (preview-first, idempotent) exists for optional seeding; the 7.8 GB
  quote ladder is excluded by default. Operator decided NOT to bulk-migrate.
- Dry session tally, game-day autopilot, MLB schedule merge + league filter
  in Discovery, UI overhaul — see `operations.md` sections.

## Standing cautions

- Every policy save disarms the live lane; the ritual is settings → Save
  once → **Arm last**. Server restarts always clear the latch.
- The policy advisor's band/threshold suggestions grade **realized**
  outcomes under the *old* exits; settlement grading contradicts it in the
  55–75c ML band specifically (−31pp vs price, −46% held on the big
  sample). Don't let advisor applies reopen that pocket or strip the
  profile structure (it happened once — check profiles after any advisor
  apply).
- Analysis discipline: dry is the experiment (keep single-variable);
  live is the operator's book (defensive changes any time). Judge slates
  at settlement, never mid-inning marks; the panel's Total Net includes
  open marks and looks worst exactly when deep holds are working.
- Roadmap (tiered, with file:line refs): `program-review-2026-08-01.md`.
  Top unbuilt items: dry/live fee symmetry, in-lane full-game settlement,
  US closing-line value, RE24/pitcher-change graduation into the adaptive
  exit context, Retrosheet offline corpus.
