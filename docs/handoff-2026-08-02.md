# Session handoff — 2026-08-02 (end of day)

Continues `handoff-2026-08-01.md`. This is a **local-workstation handoff**:
all ongoing work happens on this machine first.

## Working model (operator-decided)

- Develop and commit **locally on `main`**; accumulate commits freely.
- **Push to `origin/main` only when the operator explicitly says so** —
  pushing deploys the website (Render tracks `main`), so it is a deliberate
  release action, never an automatic follow-up to a commit.
- The workstation is local-first by enforced policy: its stores never touch
  Supabase; only website runs do. All analysis tools read local SQLite.
- `origin/main` currently sits at `e024554`; local commits after that are
  unpushed by design. The old branch `local/polymarket-us-research` keeps
  the granular pre-squash history.

Suite: **678 passed, 1 skipped**, ruff clean.

## State right now

- **Server is running** (port 8775, started by the operator via
  `start-workstation-app.cmd`; normal visible-window workflow restored —
  Ctrl+C in that window is the proper shutdown).
- **Autopilot is armed and active** for the Aug 2 slate; it shuts polling
  and dry automation off after the last final, as designed.
- **Live lane** (operator-tuned, real money, ~$25 allocation): reversal
  confirmation **5** readings, daily-loss stop $10, band 23–45c, all three
  lines with per-line profiles — ML/all pt 0.30; spread/all pt 0.20 (and an
  operator-added spread/late enabled profile); totals **early only** pt
  0.20 (middle/late/all disabled). Operator edits arrive frequently through
  the UI — always read the current policy from the DB before reasoning
  about behavior; never assume staged values survived.
- **Dry lane** (measurement bed): reversal readings **3**, all lines, band
  25–49c (operator-widened), totals early+middle (late blocked), pt 0.30
  everywhere, effectively uncapped losses by design.
- **The live-5 vs dry-3 reversal-readings A/B is live on tonight's slate.**

## The morning readout (first thing next session)

All of tonight's open questions are settled by one pass:

```bash
./.venv/Scripts/python.exe -m tools.backfill_event_outcomes --apply
./.venv/Scripts/python.exe -m tools.settlement_counterfactual workstation-data/polymarket-us-dry-run.db
./.venv/Scripts/python.exe -m tools.settlement_counterfactual polymarket-us-trading.db
```

Filter to the current regime by `entry_policy_json` stop_loss == 0.5 and
decompose by exit reason and line. Questions the data now answers:

1. **Reversal window: 3 vs 5 readings** — compare lanes per line. Earlier
   slice showed even 3-reading-confirmed reversals recovering (+$59 held on
   5 dry exits) but the same night the locks saved $97.58 on a weak-entry
   evening; one night flips this, the graded whole decides.
2. **Totals early/middle economics** under the new stack (first evening:
   2-0, +34% realized, one big open mark; stage guard verified — zero late
   entries). If middle grades well, the operator wants it opened on live.
3. **Spread entry quality** — spreads had a weak-entry night with the
   exits *protecting* (actual beat held). Watch whether that persists.
4. **Live ML winners-cut** — one slice showed 5/5 settlement winners
   realized at a loss (stops+reversals cut all five). Per-line
   stop-vs-hold needs the full grading before touching stop settings.

## What landed today (all in `5d4c52f` + `f5efcac`)

- **Reversal confirmation** (`reversal_confirmation_readings`, migration
  v16, UI field, journal telemetry) — the third one-quote exit leak, fixed
  the same way as the stop. Baseline it must beat: 62–65% of single-reading
  reversal sales were settlement winners, $93 giveback in one night.
- **Config API whitelist bug fixed**: `LiveTradingConfigIn` silently
  dropped fields not listed (the "readings stick at 3" mystery). Any new
  policy field MUST be added there or UI saves discard it; a test now pins
  this.
- **Journal retention fixed**: rejection dedup keys normalize numbers;
  qualification chatter evicts before marks. Mark evidence (edge paths,
  now run-expectancy/pitcher shadow) survives for replays.
- **MLB shadow telemetry**: every mark carries base/out run expectancy
  (RE24), count, batter/pitcher; pitcher changes journaled
  (kind=`shadow`). Strictly observational — first graduation candidate
  after a slate of evidence: "do adverse moves follow bullpen changes?"
- **Totals re-authorized** with stage guards after per-stage grading showed
  late-stage totals are the only consistently toxic pocket (−9 to −12pp,
  −36 to −61% held, both lanes); early/middle lean positive.
- **Storage policy enforced**: hosted (website) runs persist everything to
  `DATABASE_URL` (Supabase); workstation runs discard `DATABASE_URL` at
  startup and stay on local SQLite always. `tools/migrate_to_supabase.py`
  (preview-first, idempotent) exists for optional seeding; the 7.8 GB
  quote ladder is excluded by default. Operator decided NOT to bulk-migrate.
- Dry session tally, game-day autopilot, MLB schedule merge + league filter
  in Discovery, UI overhaul — see `operations.md` sections.

## Standing cautions

- Every policy save disarms the live lane; the ritual is settings → Save
  once → **Arm last**. Server restarts always clear the latch.
- The policy advisor's band/threshold suggestions grade **realized**
  outcomes under the *old* exits; settlement grading contradicts it in the
  55–75c ML band specifically (−31pp vs price, −46% held on the big
  sample). Don't let advisor applies reopen that pocket or strip the
  profile structure (it happened once — check profiles after any advisor
  apply).
- Analysis discipline: dry is the experiment (keep single-variable);
  live is the operator's book (defensive changes any time). Judge slates
  at settlement, never mid-inning marks; the panel's Total Net includes
  open marks and looks worst exactly when deep holds are working.
- Roadmap (tiered, with file:line refs): `program-review-2026-08-01.md`.
  Top unbuilt items: dry/live fee symmetry, in-lane full-game settlement,
  US closing-line value, RE24/pitcher-change graduation into the adaptive
  exit context, Retrosheet offline corpus.

## Post-slate addendum (late evening, operator-directed)

After the last final (server stopped ~21:46, autopilot had already disabled
automation on its way out), the operator directed applying the
profit-optimization findings. What changed:

- **`reversal_confirmation_readings` is a per-line profile field** (code +
  UI + tests). Effective value snapshots into `entry_policy_json` at entry
  like every profile field. Requires this code at server start; older
  runtimes cannot parse payloads carrying it.
- **`tools/apply_line_profiles.py`** — offline profile apply with embedded
  evidence-backed payloads, preview default, exact `_save_policy` shape,
  session boundary (`external_profile_apply`), running-server guards.
- **Both lanes now carry the graded-record profiles** (applied server-down;
  they load on next start): ML px 0.15–0.45 / max_edge 0.10 / stop 0.65 /
  target 0.40 / readings 5 · spread stop 0.60 / target 0.30 / readings 3 ·
  totals early+middle px 0.15–0.38 / min_edge 0.10 / target 0.30 /
  readings 3, late blocked. Evidence:
  `docs/mlb-line-profile-optimization-2026-08-02.md` (+ Aug 1–2 holdout:
  +18.7% → +53.0% per $1 simulated).

Morning sequence stays: backfill → counterfactuals → readout of tonight's
(untouched) 3-vs-5 sample → start server (profiles load; verify the twelve
resolved combinations in the settings panel) → enable → **Arm last**. Note
the dry ML profile pins readings 5 going forward, so the lane-wide A/B ends
with tonight's sample; per-line telemetry replaces it.

Post-audit revision (2026-08-03, operator-directed): a trade-by-trade audit
of the Aug 2 slate showed the model-reversal exit selling 8 eventual
settlement winners even at 5 confirmed readings (+$29.10 forgone; 4 of 7
tracked sales recovered to entry within 30 minutes). ML and spread reversal
windows are now at the maximum (readings 10) on both lanes — the wide stops
own disaster protection; the reversal exit is near-vestigial on those
lines. Totals keep readings 3 (reversal exits save money there). Applied
via tools/apply_line_profiles.py, server stopped; loads on next start.

## Final state (2026-08-03, after the full-path correction)

The addenda above are superseded in two places by later findings, in order:

1. **Truncated-marks correction** (`cebe856`): every target/stop sim that
   used tracked marks was inflated — marks stop at the actual exit. Full
   paths (marks + exit_recovery worst/best): guards-off was positive on all
   eight measured lane-nights (live −$15.09 actual → +$86.01 with targets,
   +$127.22 pure hold; dry −$94.23 → +$478.34 / +$746.78) while every
   price-triggered exit lost to settlement on ML/spread.
2. **Guards-off applied** (`c59535a`): ML/spread stop_loss 0.95
   (vestigial) + readings 10, targets kept (0.40/0.30), both lanes.
   Totals guards stay — measured protective.
3. **Totals side gate** (`efc3b14`): the totals model is an Unders model
   (Overs 16.8% win / −57.6% per $1 in every cut; Unders 64.8% / +85.8% in
   every cut; Over winners were 10 blowout games with entry signals no
   better — conviction inversely predictive). New lane field
   `allowed_total_sides`; both lanes Under-only; dry also widens Unders
   (totals min_edge 0.06, target 0.60).

**Server restart required before the next slate**: the running morning
server predates these commits — the side gate is inert until restart and
any old-runtime save drops it (re-apply with
`tools/apply_line_profiles.py --lane X --total-sides under --apply`).
After restart: verify stops 95/95/50, readings 10/10/3, "Game-total sides"
= Under only; enable → Save once → Arm last. Tripwires: any live night
≤ −$15 or two cumulative ≤ −$25 → revert exits (tool, one command).
Suite 690 passed; commits local through `efc3b14`, unpushed by design.

## Late additions (2026-08-03 afternoon)

- `reversal_confirmation_seconds` (wall-clock floor, 300s applied) decouples
  exit windows from cycle speed; both lanes run 7s cycles for venue-dip
  catching. Live ML enters on the first fresh signal
  (entry_confirmation_readings 1 — the 92 never-reconfirmed candidates
  graded +142.7%/$1 at reading-1 prices).
- **The spread model is a dog model**: favorites 26.6% win/−49% at −2.5;
  underdogs 76.1% win/+96.7%/$1 in every cut. `allowed_spread_sides`
  shipped (mirror of the totals gate). Target: Under-only + underdog-only
  on both lanes — REQUIRES a server restart onto d2f1571+, then
  `--total-sides under --spread-sides underdog` applies (an older runtime's
  save silently drops the fields).
- Dry band probes: ML floor 0.10, spread-dog floor 0.15, Unders ceiling
  0.45. Live bands hold until dry grades.
